# 📝 Story Plugin 패치 노트 (오류 수정 및 성능 최적화)

이 문서는 코틀린 마크 서버 스토리 플러그인의 심각한 오류와 성능 저하 문제를 해결하기 위해 적용된 수정 사항을 상세히 설명합니다.

---

## 🚀 1. 성능 최적화 (Performance Optimization)

### **[핵심] 실시간 디스크 I/O 제거 및 인메모리 캐싱 도입**
- **기존 문제**: 플레이어가 블록을 부수거나 아이템을 제작할 때마다 `progress.yml` 파일을 통째로 읽고 쓰는 작업이 발생했습니다. 이는 동시 접속자가 늘어날수록 서버를 멈추게(TPS 저하) 하는 주범이었습니다.
- **수정 사항**: `StoryProgressStore` 클래스에 **ConcurrentHashMap** 기반의 캐싱 시스템을 도입했습니다.
    - 플러그인이 켜질 때 (`onEnable`) 데이터를 메모리로 한 번 로드합니다.
    - 모든 데이터 변경은 메모리에서 즉시 처리되어 랙이 발생하지 않습니다.
    - 데이터 저장은 플러그인이 꺼질 때 (`onDisable`) 또는 수동 저장 시에만 안전하게 수행됩니다.

### **ItemsAdder 리플렉션 캐싱**
- **기존 문제**: 아이템 식별을 위해 매번 리플렉션을 사용하여 ItemsAdder API를 호출했습니다. 인벤토리 클릭과 같은 잦은 이벤트 발생 시 오버헤드가 컸습니다.
- **수정 사항**: 클래스와 메서드 정보를 한 번만 찾아 메모리에 저장(Caching)하여 호출 속도를 극대화했습니다.

---

## ⚖️ 2. 스토리 진행 로직 수정 (Logic Fixes)

### **선형 스토리 진행 강제 (Linear Story Flow)**
- **기존 문제**: 플레이어의 현재 진행 상태와 상관없이 모든 스토리 파일의 트리거가 동시에 작동하여 퀘스트가 뒤섞이는 현상이 있었습니다.
- **수정 사항**: `StoryRuntime`에서 플레이어의 **현재 진행 중인 스토리 ID만 체크**하도록 변경했습니다.
    - 플레이어에게 설정된 스토리가 없다면 첫 번째 스토리부터 자동으로 시작됩니다.
    - 현재 스토리의 목표(Objective)를 달성해야만 다음 스토리의 트리거가 활성화됩니다.

---

## 🛡️ 3. 안정성 및 예외 처리 (Stability)

### **초기화 순서 및 Lateinit 오류 해결**
- **기존 문제**: 플러그인 로드 도중 데이터가 준비되지 않은 상태에서 이벤트가 발생하거나 명령어가 입력될 경우 `UninitializedPropertyAccessException`으로 서버가 크래시되는 문제가 있었습니다.
- **수정 사항**: `onEnable` 시 데이터 로드 순서를 재배치하고, 명령어 실행 및 탭 완성 시 데이터 준비 상태를 안전하게 확인하도록 보완했습니다.

### **안전한 플러그인 재시작 로직**
- **기존 문제**: `/story restart` 명령어 사용 시 서버 엔진의 플러그인 관리 기능을 잘못 호출하여 재시작이 실패하거나 데이터가 유실될 위험이 있었습니다.
- **수정 사항**: 내부 `onDisable`을 통해 데이터를 안전하게 저장한 후 다시 로직을 수행하는 안정적인 재시작 방식으로 개선했습니다.

---

## 🏗️ 4. 빌드 및 종속성 설정 (Build & Dependency)

### **Kotlin 및 Maven 설정 수정**
- **수정 사항**: 
    - 잘못 설정된 Kotlin 버전(`2.4.0-Beta1`)을 안정적인 버전(`2.1.0`)으로 수정했습니다.
    - `maven-shade-plugin` 설정을 보완하여 Kotlin 표준 라이브러리가 최종 JAR 파일에 올바르게 포함되도록 했습니다.

---

**참고**: 위 수정 사항은 기존 스토리 파일(`yml`)의 구조를 전혀 건드리지 않으므로, 기존에 작성하신 스토리 파일들을 그대로 사용하실 수 있습니다.
